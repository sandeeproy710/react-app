export default function Main () {
    return(
        <main>
            <form className="add-ingredent-form">
                <input
                type="text"
                placeholder="e.g oregeno"
                aria-label="Add Ingredent" />
                <button>Add Ingredent</button>
            </form>
        </main>
    )
}